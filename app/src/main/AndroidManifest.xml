<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs.yiqixue_mvp">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.home.question.QuestionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.home.addquestion.AddQuestionActivity"></activity>
        <activity android:name=".mvp.home.answer.AnswerActivity"></activity>
        <activity android:name=".mvp.home.addanswer.AddAnswerActivity"></activity>
        <activity android:name=".mvp.home.answerdetail.AnswerDetailActivity"></activity>
        <activity android:name=".mvp.home.comment.CommentActivity"></activity>
    </application>

</manifest>